spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ddbakes?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.bakery.inventory: DEBUG
    org.hibernate.SQL: DEBUG
